import { InjectionToken } from '@angular/core';
import { UiModalDialogRef } from './ui-modal-dialog-ref';

export const UI_MODAL_DIALOG_REF = new InjectionToken<UiModalDialogRef>(
  'UI_MODAL_DIALOG_REF'
);
